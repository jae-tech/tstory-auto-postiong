# =========================================================
# 로컬 개발 환경 설정 파일
# =========================================================
# 이 파일은 로컬 개발 시에만 사용됩니다.
# 프로덕션 배포 시에는 .env 파일을 사용하세요.
#
# 사용법:
# 1. docker-compose up -d postgres  # PostgreSQL 컨테이너 시작
# 2. pnpm run db:migrate:dev        # Prisma 마이그레이션 실행
# 3. pnpm run dev                   # 개발 서버 시작
# =========================================================

# =========================================================
# 애플리케이션 기본 설정
# =========================================================
NODE_ENV=development
PORT=3000

# =========================================================
# 데이터베이스 연결 정보 (로컬 Docker PostgreSQL)
# =========================================================
# Prisma가 사용하는 DATABASE_URL
# 형식: postgresql://[사용자명]:[비밀번호]@[호스트]:[포트]/[데이터베이스명]
#
# 로컬 개발 시: localhost 사용 (호스트에서 Docker 컨테이너 접속)
# Docker 컨테이너 내부: postgres 사용 (Docker 네트워크 내부 호스트명)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tstory_automation"

# Docker Compose에서 사용할 개별 환경 변수
DB_NAME=tstory_automation
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# =========================================================
# 크롤러 설정
# =========================================================
# 크롤링할 알뜰폰 사이트 URL
CRAWLER_TARGET_URL=https://example.com/mvno-plans

# Playwright 설정 (로컬 개발)
PLAYWRIGHT_HEADLESS=false
# PUPPETEER_EXECUTABLE_PATH는 로컬에서는 자동 감지

# =========================================================
# AI 분석기 설정 (Google Gemini)
# =========================================================
# Google AI Studio에서 발급받은 API 키
# https://makersuite.google.com/app/apikey
GEMINI_API_KEY=AIzaSyDwr04r_N4_dGRD0I-QxDZp98lni1q2bVg

# 사용할 모델 (기본값: gemini-2.5-flash-lite)
GEMINI_MODEL=gemini-2.5-flash-lite

# =========================================================
# 티스토리 계정 정보
# =========================================================
# 티스토리 로그인 정보 (자동 포스팅용)
TISTORY_ID=01096666339
TISTORY_PASSWORD=rhrnak1!

# 포스팅할 블로그 URL
TISTORY_BLOG_URL=https://yourblog.tistory.com

# =========================================================
# 크론 스케줄 설정 (선택사항)
# =========================================================
# Cron 표현식으로 자동 실행 주기 설정
# 예: "0 9 * * *" = 매일 오전 9시
# 예: "0 */6 * * *" = 6시간마다
# CRON_CRAWLING_SCHEDULE=0 9 * * *
# CRON_ANALYSIS_SCHEDULE=0 10 * * *
# CRON_PUBLISHING_SCHEDULE=0 11 * * *

# =========================================================
# 로깅 설정 (선택사항)
# =========================================================
# 로그 레벨: debug, info, warn, error
LOG_LEVEL=debug
