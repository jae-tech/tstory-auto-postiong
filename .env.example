# =========================================================
# 환경 변수 템플릿 파일
# =========================================================
# 이 파일을 복사하여 실제 환경에 맞게 수정하세요:
# - 로컬 개발: .env.development (이미 생성됨)
# - 프로덕션: .env
#
# 사용법:
# cp .env.example .env
# vi .env  # 실제 값으로 수정
# =========================================================

# =========================================================
# 애플리케이션 기본 설정
# =========================================================
NODE_ENV=production
PORT=3000

# =========================================================
# 데이터베이스 연결 정보
# =========================================================
# Prisma DATABASE_URL (PostgreSQL)
# 형식: postgresql://[사용자명]:[비밀번호]@[호스트]:[포트]/[데이터베이스명]
#
# 로컬 개발: postgresql://postgres:postgres@localhost:5432/tstory_automation
# 프로덕션: postgresql://user:password@production-host:5432/dbname
DATABASE_URL="postgresql://postgres:your_password_here@localhost:5432/tstory_automation"

# Docker Compose용 개별 환경 변수
DB_NAME=tstory_automation
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# =========================================================
# 크롤러 설정
# =========================================================
# 크롤링할 알뜰폰 사이트 URL
CRAWLER_TARGET_URL=https://example.com/mvno-plans

# Playwright 설정
PLAYWRIGHT_HEADLESS=true

# =========================================================
# AI 분석기 설정 (Google Gemini)
# =========================================================
# Google AI Studio에서 API 키 발급: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# =========================================================
# 티스토리 계정 정보
# =========================================================
TISTORY_ID=your_tistory_id
TISTORY_PASSWORD=your_tistory_password
TISTORY_BLOG_URL=https://yourblog.tistory.com

# =========================================================
# 크론 스케줄 설정 (선택사항)
# =========================================================
# CRON_CRAWLING_SCHEDULE=0 9 * * *
# CRON_ANALYSIS_SCHEDULE=0 10 * * *
# CRON_PUBLISHING_SCHEDULE=0 11 * * *

# =========================================================
# 로깅 설정 (선택사항)
# =========================================================
# LOG_LEVEL=info